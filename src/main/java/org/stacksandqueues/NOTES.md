### Stacks and queues

 - A stack uses LIFO (last in first out), similar to the approach used by JS arrays.
 - In JS arrays we push (to the last) and pop (the last)

 - Queues use FIFO (first in first out)

## 